/* Boîte des textos : feuille de style propre à la section. La session est vérifiée dans chaque page et action. */
import "@/components/gestion/textos/textos.css";

export default function TextosLayout({ children }: { children: React.ReactNode }) {
  return children;
}
